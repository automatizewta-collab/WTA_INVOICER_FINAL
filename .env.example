# Database URL
# SQLite (development):
DATABASE_URL=file:../db/custom.db

# MySQL (production) - uncomment below and comment SQLite:
# DATABASE_URL=mysql://user:password@localhost:3306/invoicer

# ── ERP / MSSQL Connection (optional - uses mock data if not set) ──
# ── ERP / MSSQL Connection (optional - uses mock data if not set) ──
MSSQL_HOST=192.168.1.13
MSSQL_PORT=1433
MSSQL_USER=automacao
MSSQL_PASSWORD=wT@v3t
MSSQL_DATABASE=CORPORATIVO_PRODUCAO
MSSQL_ENCRYPT=false